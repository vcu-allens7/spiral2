def spiralize(size, n=1):
    #create an empty list
    spiral2 = [] 
    for x in range (0,501)
        #something that chooses only the diagonals
        spiral2.append(x)
    n = sum(spiral2)
    return_value = n
    return return_value
